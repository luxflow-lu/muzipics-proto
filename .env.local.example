# Rename to .env.local and set your tokens
# Replicate is currently not used (switched to Hugging Face SDXL)
# REPLICATE_API_TOKEN=r8_...

# Hugging Face Inference API token (read-only)
# Create at https://huggingface.co/settings/tokens
HF_API_TOKEN=hf_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Vercel Blob read-write token (for saving generated images)
# Create in Vercel > Storage > Blob (token scope: read/write)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXXXXXXXXXXXXXXXXXX